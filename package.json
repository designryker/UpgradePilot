{
  "name": "upgradepilot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "node tools/smoke-polish.mjs",
    "check": "npm run build && npm run smoke"
  },
  "devDependencies": {
    "vite": "^7.2.7"
  }
}
