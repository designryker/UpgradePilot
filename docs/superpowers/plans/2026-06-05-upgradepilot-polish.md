# UpgradePilot Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Polish UpgradePilot's existing UI and recommendation flow for clarity, readability, and confidence.

**Architecture:** Keep the current Vite app and large-file structure. Extract only small pure helpers into `src/recommendation-helpers.js` so search matching and BIOS gating can be smoke-tested without a browser.

**Tech Stack:** Vanilla HTML/CSS/JS, Vite, Node smoke tests.

---

### Task 1: Testable Recommendation Helpers

**Files:**
- Create: `src/recommendation-helpers.js`
- Create: `tools/smoke-polish.mjs`
- Modify: `src/main.js`

- [ ] Add failing smoke tests for GPU/CPU aliases and BIOS recommendation gating.
- [ ] Create `normalizePartSearch`, `buildPartSearchText`, and `getBiosRecommendation`.
- [ ] Import helpers in `src/main.js` and use them for search and checklist generation.
- [ ] Run `node tools/smoke-polish.mjs`.

### Task 2: Result Copy and Free Optimization

**Files:**
- Modify: `index.html`
- Modify: `src/i18n.js`
- Modify: `src/main.js`

- [ ] Replace generic result intro copy with shorter, more specific text.
- [ ] Shorten diagnostic notes, why text, action-plan steps, and checklist items.
- [ ] Add conditional BIOS item only when `getBiosRecommendation` returns a recommendation.
- [ ] Remove future-facing copy from free optimization guidance.

### Task 3: Card and Hover Polish

**Files:**
- Modify: `src/main.js`
- Modify: `src/styles.css`

- [ ] Simplify example/tier cards to title, range, one short note, and action.
- [ ] Move dense compatibility disclaimers into compact notes.
- [ ] Remove hover lift/glow from passive cards.
- [ ] Keep hover states on buttons, links, quick chips, clickable fields, PC map, and accordion headers.

### Task 4: Verification

**Files:**
- Built output generated by Vite.

- [ ] Run `node tools/smoke-polish.mjs`.
- [ ] Run `npm run build`.
- [ ] Review the diff for scope creep.
